import React from "react";
interface NoDataProps {
    noDataText?: string;
    className?: string;
}
declare const NoData: ({ className, noDataText }: NoDataProps) => React.JSX.Element;
export default NoData;
