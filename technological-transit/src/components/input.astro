---
interface Props {
placeholder: string;
type:"text" | "email" | "password" | "number";
}

const { placeholder, type} = Astro.props;

---

<div class="flex items-center"> 
<label></label>
            <input type={type} id="correo" placeholder={placeholder} name="telefono" required class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-blue-500">
</div>